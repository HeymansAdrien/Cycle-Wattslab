# Training Data
Place your cycling training files here in `.tcx` format. 

The `power_curve.py` script will automatically scan this folder and process all files it finds. Ensuring that files contain power data (Watts) for proper analysis.
